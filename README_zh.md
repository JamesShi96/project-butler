# project-butler

[English](README.md) | **[中文](README_zh.md)**

> 一条命令，让你的项目拥有持久记忆。

当你用 AI 编程助手跨多个 session 工作时，每次都要从零开始。AI 不记得昨天发生了什么、做过哪些决策、项目的规则是什么。你不得不反复解释上下文、重新建立规范，大量的项目知识就这么流失了。

**project-butler** 通过一条命令搭建一套自维护的项目管理系统。设置完成后，AI 助手会自动记录会话日志、维护项目百科、追踪任务进度、积累规则候选——不需要你手动维护。

## 解决什么问题

如果你遇到过这些情况，这个 skill 就是为你准备的：

- **"我今天第三次给 AI 重新解释项目架构了"** — 每个新 session 都从零开始，没有上下文
- **"上周关于命名规范我们是怎么决定的？"** — 决策在 session 之间丢失了
- **"改完东西总是忘了更新 README"** — 文档和实际渐行渐远
- **"AI 总是违反我的项目规则"** — 规则只在你脑子里，没有写到 AI 能读到的地方
- **"我同时用 Claude Code 和 Cursor，上下文无法互通"** — 不同工具，不同上下文

## 工作原理

运行一次 `/project-butler`，它会创建 6 个文件，按 4 个层级组织：

```
project-root/
├── CLAUDE.md                   ← 项目宪法（人工确认的规则）
├── PROJECT.md                  ← 项目 Wiki（AI 自动同步）
├── session-handoff.md          ← 跨会话接手指引（AI 自动更新）
├── TODO.md                     ← 执行清单
├── log/                        ← 会话日志（AI 自动生成）
└── .claude/
    └── candidates.md           ← 宪法候选池（AI 自动收集）
```

### 设计哲学：按稳定性分层

```
  ┌─────────────────────────────────────┐
  │  CLAUDE.md（项目宪法）              │  ← 很少改动，人工审批
  │  ↑ AI 自动收集候选条目              │
  └─────────────────────────────────────┘
            ↑ 从下层抽象沉淀
  ┌─────────────────────────────────────┐
  │  PROJECT.md（项目 Wiki）            │  ← 自动同步的当前快照
  │  项目"是什么"、"到哪了"             │
  └─────────────────────────────────────┘
            ↑ 从下层状态汇总
  ┌──────────────────────┐ ┌───────────────────────┐
  │  log/（会话日志）     │ │  TODO.md（任务清单）   │  ← 原始事实，频繁写入
  │  发生了什么、什么时候  │ │  需要做什么            │
  └──────────────────────┘ └───────────────────────┘
```

**下层喂养上层，上层约束下层。**

- **日志和 TODO** 是原始事实流——发生了什么、需要做什么
- **Wiki** 是当前状态快照——从原始事实中自动提炼
- **宪法** 是稳定原则层——持续生效、指导所有行为的规则

这个层级意味着信息向上流动（事实 → 快照 → 原则），约束向下传递（原则 → 快照 → 事实）。每一层有不同的变更频率、维护人和用途。

### 语言支持

project-butler 支持 3 种语言模式：

| 模式 | 内容语言 | 用户文件命名 |
|------|---------|-------------|
| `en` (英文) | 全部英文 | 英文命名（kebab-case） |
| `zh` (中文) | 全部中文 | 允许中文命名 |
| `bilingual` (默认) | 中文为主，英文标注 | 英文优先，中文可接受 |

在 `/project-butler` 设置时选择，或随时说"切换语言" / "change language"更改。

切换语言时，会询问是否按新语言的命名规范重命名用户文件。系统文件（CLAUDE.md, PROJECT.md 等）始终保持英文名。

### 四大组件

#### 1. 会话日志（log/）

每次你说 `end session`，AI 自动写一条结构化日志：

```markdown
# Session 2026-04-21 — PRD 草稿

## 本次目标
## 关键操作（按时间顺序）
## 决策与理由
## 产出文件
## 未完事项 / 下次接手点
## 候选 CLAUDE.md 条目（如有）
```

像 meeting notes，但是全自动的。下一个 session，AI 读取最新日志就能接上。

#### 2. 项目 Wiki（PROJECT.md）

一个文件回答：**这个项目是什么，现在进展到哪了？**

- 一句话定义、当前阶段、模块地图
- 文件结构（自动生成）
- 关键文件索引及说明
- 进度快照表
- 相关链接（GitHub、文档、设计稿）

文件变化或会话结束时自动同步。任何人（或 AI）读这一个文件就能理解项目全貌。

#### 3. 项目宪法（CLAUDE.md）

项目的规则和边界——带有一个关键的安全机制：

- **AI 绝对不直接修改 CLAUDE.md**
- 工作过程中，AI 识别出可能是规则的模式（"这个用户总是要先写测试"、"他们用 snake_case 命名文件"），收集到 `.claude/candidates.md`
- 你说 `review claude` 时，AI 逐条展示候选条目，让你**确认、驳回或改写**
- 只有你确认的条目才会写入 CLAUDE.md

这意味着宪法从真实使用模式中有机生长，但你始终掌控最终决定权。

#### 4. 执行清单（TODO.md）

不是灵感池，是执行计划。每条任务必须包含三个要素：

```
- [ ] 实现用户认证
  负责人：James｜截止：2026-04-30｜依赖：数据库 schema 定稿
```

你提到任务但缺要素时，AI 会主动追问补全。完成的任务勾选保留（不删除），作为执行历史。

## 触发词

设置完成后，这些触发词成为你的日常工作流：

| 你说（任何表达...意思的话） | 发生什么 |
|---------|-------------|
| "做完了" / "收工" | 写 log + 更新 handoff + 同步 Wiki + 整理文件 + 输出总结 |
| "审查规则" / "更新宪法" | 展示候选规则逐条确认 |
| "同步一下" / "同步项目" | 强制重扫并更新 PROJECT.md |
| "项目现状" / "进度怎么样" | 朗读 Wiki + handoff 摘要 |
| "整理文件" / "收拾一下" | 扫描并按 STRUCTURE.md 规则整理文件 |
| "切换语言" / "换成英文" | 更改所有管理文件的内容语言 |

核心体验：**你正常工作，做完说一声 "end session"，系统自动处理剩下的一切。**

## 多工具支持

如果你同时使用 Claude Code 和 Cursor，`/project-butler` 可以选择创建 `.cursor/rules/project-system.mdc` 文件，镜像相同的触发行为。两个工具读取同一份 `PROJECT.md` 和 `session-handoff.md`，上下文无缝衔接。

## 安装

```bash
# Clone 到你的 Claude Code skills 目录
git clone https://github.com/JamesShi7/project-butler.git ~/.claude/skills/project-butler
```

## 使用

在任意项目目录下：

```
/project-butler
```

回答 5 个问题（项目名称、一句话描述、当前阶段、GitHub 地址、是否创建 Cursor 规则），所有文件自动创建。

**升级模式**：如果项目已经有部分文件（CLAUDE.md、TODO.md 等），只会创建缺失的文件，绝不覆盖已有内容。同时会检测旧版 `.claude/memory/` 目录并建议迁移。

## 环境要求

- [Claude Code](https://claude.com/claude-code) CLI
- 可选：[Cursor](https://cursor.sh)（用于跨工具规则文件）

## 许可证

MIT
